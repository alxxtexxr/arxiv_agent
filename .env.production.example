ARXIV_AGENT_PORT=80
ARXIV_RECOMMENDATION_CATEGORY="cs.AI"

# Key configuration
LANGSMITH_API_KEY=
OPENAI_API_KEY=
MISTRAL_API_KEY=
OPENROUTER_API_KEY=

# LangChain configuration
LANGCHAIN_TRACING="false"
LANGCHAIN_TRACING_V2="false"

# Chat model configuration
# CHAT_MODEL must use the format 'model_provider:model_name'
# For OpenRouter models, reasoning must be enabled to use tools, so set CHAT_MODEL_REASONING_EFFORT
CHAT_MODEL="openai:gpt-5.6-luna"
# CHAT_MODEL="mistral:mistral-medium-latest" # Free alternative
# CHAT_MODEL="openrouter:openrouter/free" # Free alternative
CHAT_MODEL_REASONING_EFFORT="medium"

# GitHub bookmarks configuration
GITHUB_TOKEN=
GITHUB_BOOKMARKS_URL=

# ================================
# Production configuration
# ================================
# Checkpointer database configuration
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="postgres" # Database created when the postgres service is initialized
DATABASE_URI="postgres://postgres:postgres@postgres:5432/postgres"

# Vector database configuration
PGVECTOR_USER="postgres"
PGVECTOR_PASSWORD="postgres"
PGVECTOR_DB="arxiv_agent"
VECTOR_DATABASE_URI="postgres://postgres:postgres@postgres:5432/arxiv_agent"

# Embedding configuration
EMBEDDING_INTEGRATION="fastembed" # 'openai' | 'fastembed' | 'tei' | 'hf' or 'huggingface'
EMBEDDING_MODEL="BAAI/bge-small-en-v1.5"
TEI_EMBEDDINNG_URL=
ENABLE_EMBEDDING_CACHE="true" # 'true' | 'false' — in-memory embedding model cache

# Reranking configuration
RERANKER_INTEGRATION="flashrank" # 'flashrank' | 'tei' | 'hf' or 'huggingface'
RERANKER_MODEL="ms-marco-MiniLM-L-12-v2"
TEI_RERANKER_URL=
ENABLE_RERANKER_CACHE="true" # 'true' | 'false' — in-memory reranker model cache
